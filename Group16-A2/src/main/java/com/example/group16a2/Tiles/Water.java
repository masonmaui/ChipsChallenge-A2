package com.example.group16a2.Tiles;

public class Water extends Tile{

        public Water(String ID, boolean isInteractable) {
            super(ID, isInteractable);
        }

        //method to get the ID of the tile
        public String getID() {
            return super.getID();
        }
}
